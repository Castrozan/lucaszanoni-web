{
  "name": "@platform/ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "dist",
    "server-entrypoint.mjs"
  ],
  "scripts": {
    "build": "esbuild src/server.ts --bundle --platform=node --format=esm --target=node22 --outfile=dist/server.mjs",
    "start": "node server-entrypoint.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@platform/ingestion-contracts": "workspace:*"
  },
  "devDependencies": {
    "@platform/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3",
    "vitest": "^4.1.9"
  }
}
